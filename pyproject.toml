[project]
name = "db-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42.85",
    "duckdb>=1.5.1",
    "huggingface-hub>=1.10.1",
    "pandas>=3.0.2",
    "prefect>=3.6.25",
    "pyarrow>=23.0.1",
]
